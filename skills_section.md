**编程语言**：Python

**AI/LLM 框架**：LangGraph、LangChain、Prompt Engineering、RAG（检索增强生成）

**LLM 平台**：DeepSeek（deepseek-chat / v4-pro）、Anthropic Claude、DashScope

**向量与检索**：ChromaDB、DashScope Text Embedding v2、BAAI bge-small-zh-v1.5、语义检索引擎构建

**后端与 Web**：FastAPI、Streamlit、SSE 流式输出、RESTful API

**数据存储**：Redis、SQLite、Chroma、三层降级存储设计

**数据采集**：网络爬虫、链接验证、数据清洗

**可视化**：Plotly、Streamlit 前端交互

**工程实践**：多 Agent 协作编排、Agent 权限控制、多层容错降级、JSON 解析与校验、超时重试与异常兜底、全链路防御式编程
